# Phase 1 -- tracker only. This is all you need right now.
hidapi==0.14.0.post4     # imports as `hid`; bundles the DLL in the Windows wheel

# Phase 2 -- orientation filter.
numpy>=1.24

# Phase 3 -- 360 video renderer. Install when we get there.
# moderngl>=5.8
# glfw>=2.6
# opencv-python>=4.9
# pyglm>=2.7
